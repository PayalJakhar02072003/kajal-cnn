# Streamlit Community Cloud (Python 3.14+): TensorFlow has no wheels there yet.
# The app loads saved_model/mnist_cnn.onnx with onnxruntime — no TensorFlow at runtime.
onnxruntime>=1.18.0
numpy>=1.24.0
Pillow>=10.0.0
