# Local only: training, ONNX export, Streamlit, optional Keras fallback.
# Install after base:  pip install -r requirements.txt && pip install -r requirements-dev.txt
# Or one shot:        pip install -r requirements-dev.txt
streamlit>=1.28.0
tensorflow-cpu>=2.15.0,<2.19.0
onnxruntime>=1.18.0
tf2onnx>=1.16.0
onnx>=1.14.0
numpy>=1.24.0
Pillow>=10.0.0
