# tensorflow-cpu: same API, works on Windows/Linux (Streamlit Cloud is Linux).
# On Python 3.13+, use `pip install -U tensorflow` if this pin blocks resolution.
tensorflow-cpu>=2.15.0,<2.19.0
streamlit>=1.28.0
numpy>=1.24.0
Pillow>=10.0.0
